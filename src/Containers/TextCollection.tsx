import React from 'react';
import TextCollection from '../Screens/TextCollection';

const TextCollectionContainer = () => {
  return (
    <TextCollection />
  );
};

export default TextCollectionContainer;
